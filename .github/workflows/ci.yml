name: CI

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main, develop]

jobs:
  ci:
    uses: 1mb-dev/.github/.github/workflows/ci-go.yml@main
    with:
      go-versions: '["1.26"]'
      lint-go-version: "1.26"
      golangci-lint-version: "v2.9.0"
      build-binary: "./cmd/markgo"
